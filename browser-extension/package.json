{
  "name": "devlog-browser-extension",
  "private": true,
  "version": "1.0.0",
  "description": "Tests for the devlog browser extension",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "jsdom": "^29.0.0",
    "vitest": "^4.0.0"
  }
}
